"""auth-svc — local subset (in-memory session manager).

The full auth-svc epic lives in issue #5. This package contains the
local subset: the SessionManager interface, the InMemorySessionManager
concrete class, and the Session data model. See DESIGN.md for the
design rationale, the RED test spec, and the scope of what is and
isn't covered by the local subset.
"""
