# OS
.DS_Store
Thumbs.db
desktop.ini

# Python
__pycache__/
*.py[cod]
*$py.class
*.so
.Python
.venv/
venv/
env/
ENV/
.pytest_cache/
.mypy_cache/
.ruff_cache/
.coverage
htmlcov/
*.egg-info/
dist/
build/

# IDE / editor
.idea/
.vscode/
*.swp
*.swo
*~
.project
.classpath
.settings/

# Node / web tooling
node_modules/
.next/
out/
.turbo/
.parcel-cache/
.eslintcache

# Secrets / local config
.env
.env.local
.env.*.local
*.pem
*.key
secrets/

# OS temp
$RECYCLE.BIN/

# Build artifacts we explicitly DO NOT want to keep
*.log
*.tmp
*.bak

# Operator-side dev inputs — chat logs, prompts, scratch notes used to
# generate the docs. These are not part of the product repo. (See
# ChatHistory.txt, which was accidentally committed in the bootstrap
# and is still tracked on main; that file is being cleaned up in a
# separate PR — this rule prevents future ones from being added.)
/ChatHistory.txt
/ai-plane-scratch/
/scratch/
/notes/

# Cycle intermediates (per-run artifacts, not audit records)
# These are the pre_*/post_*/put_*/open_*/delete_*/merge_pr{N}.json files
# that each Option 3 cycle produces. The audit records (merge_pr*_audit.json)
# ARE committed; the per-run outputs are not.
/pre_*.json
/post_*.json
/put_*.json
/open_prs.json
/delete_reviews*.json
/merge_pr[0-9]*.json
!/merge_pr*_audit.json
