# OS
.DS_Store
Thumbs.db
desktop.ini

# Python
__pycache__/
*.py[cod]
*$py.class
*.so
.Python
.venv/
venv/
env/
ENV/
.pytest_cache/
.mypy_cache/
.ruff_cache/
.coverage
htmlcov/
*.egg-info/
dist/
build/

# IDE / editor
.idea/
.vscode/
*.swp
*.swo
*~
.project
.classpath
.settings/

# Node / web tooling
node_modules/
.next/
out/
.turbo/
.parcel-cache/
.eslintcache

# Secrets / local config
.env
.env.local
.env.*.local
*.pem
*.key
secrets/

# OS temp
$RECYCLE.BIN/

# Build artifacts we explicitly DO NOT want to keep
*.log
*.tmp
*.bak

# Operator-side dev inputs — chat logs, prompts, scratch notes used to
# generate the docs. These are not part of the product repo. (See
# ChatHistory.txt, which was accidentally committed in the bootstrap
# and is still tracked on main; that file is being cleaned up in a
# separate PR — this rule prevents future ones from being added.)
/ChatHistory.txt
/ai-plane-scratch/
/scratch/
/notes/
